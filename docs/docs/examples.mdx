---
title: Examples
---

import siteConfig from '/docusaurus.config.js';

# Examples to get you started

## Create a new grid

A grid is created and data is set in one command. Data can be an array of objects, an array of arrays or a mixed array of objects, arrays and primitives.'

<SandpackEditor>

```ts
import data from '/data.json';
import canvasDatagrid from 'canvas-datagrid';

// TODO: figure out cleaner boilerplate
const parentNode = document.createElement('div');
const grid = canvasDatagrid({
  parentNode,
  data,
});
const currentDiv = document.getElementById('div1');
document.body.insertBefore(parentNode, currentDiv);
```

```json data.json
[
  { "col1": "foo", "col2": 0, "col3": "a" },
  { "col1": "bar", "col2": 1, "col3": "b" },
  { "col1": "baz", "col2": 2, "col3": "c" }
]
```

</SandpackEditor>
